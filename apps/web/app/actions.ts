'use server'

export async function incrementCounter(): Promise<void> {
  // Proves the Server Action pipeline compiles and executes.
  // DB connectivity is validated by the feature integration tests.
}
